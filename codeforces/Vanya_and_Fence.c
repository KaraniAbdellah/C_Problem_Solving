#include <stdio.h> 

int main() {
    int nbr_friends, height_fence;
    scanf("%d %d", &nbr_friends, &height_fence);
    int T[1000];
    for (int i = 0; i < nbr_friends; i++) {
        scanf("%d", &T[i]);
    }
    return 0;
}

